node_modules/
dist/
.astro/
.env
.env.*

# Crawler caches (rebuildable from tracked ledgers + records)
data/http_cache/
data/raw_content/
data/diagrams/
data/extract_jobs/

# Build artifacts (rebuildable from data/records/)
data/usecase_intel.sqlite
data/*.sqlite
data/*.sqlite-*
data/case-studies.merged.json
data/case-studies.generated.json

# Tracked (do NOT ignore — these persist across CI runs and drive resumability):
#   data/records/**                — authored normalized records
#   data/discovered_urls.jsonl     — URL queue with sitemap lastmod
#   data/fetch_manifest.jsonl      — fetch ledger

__pycache__/
*.pyc